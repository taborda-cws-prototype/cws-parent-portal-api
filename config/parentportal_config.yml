applicationName: CWS Parent Portal

dataSourceFactory:
  driverClass: org.postgresql.Driver
  user: ${DB_USER}
  password: ${DB_PASSWORD}
  url: ${DB_JDBC_URL}
flywayFactory:
    locations:
      - db/migration
